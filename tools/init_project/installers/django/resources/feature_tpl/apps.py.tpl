from django.apps import AppConfig


class {{APP_CLASS}}Config(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "features.{{APP_NAME}}"
    verbose_name = "{{APP_VERBOSE}}"
